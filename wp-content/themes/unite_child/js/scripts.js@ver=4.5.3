
var j = jQuery.noConflict();

j(document).ready(function() {
    // alert("The paragraph was clicked.");
    // console.log(11);
    // j('.wpcf7 form input').addClass('no-valid');

    if ( j('body.page-template-front-page-php').length > 0 )
    {
        j('.site-footer > .newsletter').css('display', 'none');
    }


    j(".wpcf7 form .wpcf7-submit").click(function(){
    // j('.wpcf7 form input').addClass('no-valid');
    //     var target = j( event.target );
    //     if ( target.is( "li" ) ) {
    //         target.css( "background-color", "red" );
    //     }
    });

    // podmiana spacji na _ i . na _ w subpage-left-menu
    // j('.menu-filters a[href^="#"]').each(function(){
    //     var newUrl = j(this).attr('href').replace(/ /g, '_').replace(/ą/g, 'a').replace(/Ą/g, 'A').replace(/ć/g, 'c').replace(/Ć/g, 'C').replace(/ę/g, 'e').replace(/Ę/g, 'E').replace(/ł/g, 'l').replace(/Ł/g, 'L').replace(/ń/g, 'n').replace(/Ń/g, 'N').replace(/ó/g, 'o').replace(/Ó/g, 'O').replace(/ś/g, 's').replace(/Ś/g, 'S').replace(/ż/g, 'z').replace(/Ż/g, 'Z').replace(/ź/g, 'z').replace(/Ź/g, 'Z').replace( /\./g ,'_').toLowerCase();
    //     j(this).attr('href', newUrl);
    // });
    //
    // j('#menu-cart section').each(function(){
    //     var newUrl2 = j(this).attr('id').replace(/ /g, '_').replace(/ą/g, 'a').replace(/Ą/g, 'A').replace(/ć/g, 'c').replace(/Ć/g, 'C').replace(/ę/g, 'e').replace(/Ę/g, 'E').replace(/ł/g, 'l').replace(/Ł/g, 'L').replace(/ń/g, 'n').replace(/Ń/g, 'N').replace(/ó/g, 'o').replace(/Ó/g, 'O').replace(/ś/g, 's').replace(/Ś/g, 'S').replace(/ż/g, 'z').replace(/Ż/g, 'Z').replace(/ź/g, 'z').replace(/Ź/g, 'Z').replace( /\./g ,'_').toLowerCase();
    //     j(this).attr('id', newUrl2);
    // });

    //    funkcja animująca przejścia między menu w karcie menu
    j('#menu-cart .menu-filters a[href^="#"]').click(function() {
        var hash = j(this).attr('href');
        console.log(hash);
        console.log(j(hash).offset().top);
        j('html, body').animate({
            scrollTop: j(hash).offset().top - 19
        }, 500);
        return false;
    });

    //    funkcja animująca przejścia w kontakcie
    j('.contact-details a[href^="#"].btn-brown-white').click(function() {
        var hash = j(this).attr('href');
        j('html, body').animate({
            scrollTop: j(hash).offset().top - 19
        }, 500);
        return false;
    });


    j('a').not('.bx-wrapper a').on('tap', function (e) {
        var link = j(this).attr('href');
        if (typeof link !== 'undefined') {
            window.location = link;
        }
    });

    j('#cn-accept-cookie').addClass('btn btn-white');


    j('.paging-navigation .container').has('.nav-previous').has('.nav-next').addClass('nav-next-prev');

    j( window ).resize(function() {
        if (screen.width <= 480) {
            j('.menu-card .container .inner h2').removeClass('decoration-large-dark');
        } else {
            j('.menu-card .container .inner h2').addClass('decoration-large-dark');
        }
    });
    if (screen.width <= 480) {
        j('.menu-card .container .inner h2').removeClass('decoration-large-dark');
    } else {
        j('.menu-card .container .inner h2').addClass('decoration-large-dark');
    }
    // IOS rozwiązanie problemu podwojnego tapu w link
    j('#menu-main-menu').on('click touchend', function(e) {
        var el = j(this);
        var link = el.attr('href');
        window.location = link;
    });

    var text = j('.languages .active a').html();
    j('.languages .current p').html('Language: ' + text);


    // skaczacy background attachment: fixed na ie i edge
    j('body').on("mousewheel", function () {
        event.preventDefault();

        var wheelDelta = event.wheelDelta;

        var currentScrollPosition = window.pageYOffset;
        window.scrollTo(0, currentScrollPosition - wheelDelta);
    });

});


// pause video if is closed modal
j(function(){
    j('#stop, .modal').click(function(){
        j('iframe').attr('src', j('iframe').attr('src'));
    });
});
